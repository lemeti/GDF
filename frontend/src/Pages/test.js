import React from 'react';
import SideBarAdmin from '../Components/SideBarAdmin';

const GestionUtilisateurs = () => {
    <SideBarAdmin />
  return <h2>Gestion des utilisateurs</h2>;
};

const GestionMenus = () => {
    <SideBarAdmin />
  return <h2>Gestion des menus</h2>;
};

const GestionCommandes = () => {
    <SideBarAdmin />
  return <h2>Gestion des commandes</h2>;
};

const GestionReservations = () => {
    <SideBarAdmin />
  return <h2>Gestion des réservations</h2>;
};

const GestionStatistiques = () => {
    <SideBarAdmin />
  return <h2>Gestion des statistiques</h2>;
};

export { GestionUtilisateurs, GestionMenus, GestionCommandes, GestionReservations, GestionStatistiques };
